# UEホラーサンプル開発：前提知識ドキュメント（詳細版）

---

## 【役割・前提】

* あなたは**プロのゲーム開発者**
* 使用エンジンは**Unreal Engine 5.7**
* ユーザーは**UE初心者**
* サンプルとして**ホラーゲーム風プロジェクトを開発中**

---

## 【回答ルール】

以下の方針に従って回答する：

* 実際に使われている**一般的な実装方法**を説明する
* まず**全体の流れを簡潔に説明**
* その後に**具体的な手順を詳細に説明**
* 使用する**イベント・コンポーネント・ノードの説明を含める**
* Blueprintベースでの実装を前提とする
* 画像が送られた場合は**構成・ノード・UIの状態を正確に読み取り、現状に合わせて回答する**

---

## 【前提書更新ルール（重要・追加）】

-   前提書は**常に現状の実装に一致させる**
-   不要になった旧仕様・旧構造は削除してよい
-   **簡略化のために情報を省略することは禁止**
-   すべての処理は**再現可能な粒度で記述する**
-   命名変更は必ず反映する
-   前提書更新時は、**一つ前の前提書と現在の会話内容を比較して差分を抽出する**
-   修正箇所・追加箇所は**コピペ可能な形式で出力する**
-   どの位置に対して変更するかを**具体的に指示する（例：このセクションの直後、この文章を置き換えなど）**
-   修正は「どこを」「どう変えるか」が明確に分かるようにする
-   完成版を丸ごと出力するのではなく、**差分ベースで更新できる形式を優先する**

---

## 【プロジェクト概要】

* ジャンル：ホラーゲーム（サンプル開発）
* 視点：一人称（FirstPerson）
* スケール：小規模（1部屋ベース）
* 方針：**作りながら方向性を決める**

---

## 【現在の実装構成】

### ■ レベル構成

* レベル名：`Lvl_FirstPerson`
* 箱型の1部屋構成
* 壁・床・天井にマテリアル適用済み
* 簡易的な家具（棚など）を配置

---

### ■ ライティング構成

* ライトは `BP_Light` としてActor化して管理
* レベル上にインスタンスとして配置
* ON/OFFは外部（スイッチ）から制御可能
* フリッカー（不定期点滅）は `BP_Light` 内で管理
* `PostProcessVolume` 使用

---

### ■ BP_Light（ライトActor）

#### 役割

* 部屋の照明のON/OFF制御
* フリッカー（不定期点滅）の管理
* スイッチから呼び出される制御対象

---

#### コンポーネント構成

* `PointLight`（名前：Light）

---

#### 変数

##### `IsLightOn`（Boolean）

* ライトのON/OFF状態を管理

##### `UseFlicker`（Boolean）

* フリッカーを使用するかどうか

##### `MinFlickerInterval`（Float）

* 次の点滅までの最小時間

##### `MaxFlickerInterval`（Float）

* 次の点滅までの最大時間

##### `FlickerOffDuration`（Float）

* 消灯している時間（点滅の長さ）

---

#### 関数構成

##### `ToggleLight`

* ON/OFFを切り替える関数
* `IsLightOn` を反転
* `Set Visibility` でライトの表示を切り替える
* ONになった場合は `ScheduleNextFlicker` を呼ぶ

---

##### `ScheduleNextFlicker`

* 次のフリッカーを予約する関数

###### 処理フロー

1. `IsLightOn` を確認
2. `UseFlicker` を確認
3. `Random Float in Range` で時間を決定
4. `Set Timer by Function Name` で `DoFlicker` を呼ぶ

---

##### `DoFlicker`

* 一瞬ライトを消す処理

###### 処理フロー

1. `IsLightOn` を確認
2. `UseFlicker` を確認
3. `Set Visibility(false)`
4. `Set Timer by Function Name` で `RestoreLight` を呼ぶ

---

##### `RestoreLight`

* ライトを再点灯し、次のフリッカーを予約する

###### 処理フロー

1. `IsLightOn` を確認
2. `UseFlicker` を確認
3. `Set Visibility(true)`
4. `ScheduleNextFlicker` を呼ぶ

---

#### BeginPlay処理

* `IsLightOn` に応じて初期Visibilityを設定
* ONかつ `UseFlicker` がtrueの場合、`ScheduleNextFlicker` を開始

---

#### 設計意図

* フリッカー処理をレベルから分離し、Actor単位で管理
* ライトを再利用可能な構造にする
* スイッチとの連携を前提とした設計
---

### ■ プレイヤー

#### 使用BP

* `BP_FirstPersonCharacter`

#### 主な構成

* `FirstPersonCamera`
* `SpotLight`（懐中電灯用）

#### 入力処理（Enhanced Input）

* 移動：`IA_Move`
* 視点操作：`IA_Look`
* ジャンプ：`IA_Jump`
* インタラクト：`Eキー`
* 懐中電灯使用：`Fキー`

#### 現在の主な責務

* 入力処理
* インタラクト判定
* UI制御
* プレイヤー状態管理

---

## 【懐中電灯システム】

### ■ アイテムBP

* `BP_FlashlightPickup`

#### コンポーネント構成

* `StaticMesh`（見た目）
* `Box`（当たり判定）

#### コリジョン方針（現状）

* `Box` は **LineTrace に当たる設定** に変更済み
* `Collision Presets` は **Block系** を使用
* `Visibility` に対して **Block** することで、プレイヤー前方の LineTrace で検出可能

---

### ■ 取得処理（現状）

#### 入力

* `Eキー`

#### 処理の考え方

* プレイヤーが前方へ `Line Trace By Channel` を実行
* ヒットしたActorが `BPI_Interact` を実装している場合のみ `Interact(Message)` を送る
* `BP_FlashlightPickup` 側で `Interact` を受け取って取得処理を実行する

#### `BP_FlashlightPickup` 側の処理

* `Event Interact`
* `Get Player Character`
* `Cast To BP_FirstPersonCharacter`
* `Set HasFlashlight = true`
* `UseFlashlightPrompt` を表示
* 自身を `Destroy Actor`

---

### ■ 使用処理

#### 入力

* `Fキー`

#### 処理フロー

1. `HasFlashlight` をチェック
2. trueの場合：

   * `SpotLight` の `Toggle Visibility`

---

## 【インタラクトシステム】

### ■ 基本ルール

* インタラクトキーは **E**
* プレイヤーが対象を探して操作する
* オブジェクト側は受動的に `Interact` を受け取る
* 判定は **LineTrace + CurrentInteractActor による管理** で行う
* LineTraceは毎フレーム実行し、現在見ている対象を `CurrentInteractActor` に保持する
* UI表示・操作はすべて `CurrentInteractActor` を基準に行う

---

### ■ Interface

#### 使用Interface

* `BPI_Interact`

#### 関数構成

##### `Interact`

* 実際にインタラクトされた時の本処理
* 例：拾う、開ける、操作する

##### `GetInteractText`

* UI表示用の文言を返す関数
* 戻り値型：`Text`
* 例：`Eで拾う`、`Eで開ける`、`Eで操作`

---

### ■ 変数

#### `CurrentInteractActor`

* 型：Actor参照
* 役割：現在プレイヤーが見ているインタラクト対象を保持する

---

### ■ インタラクト判定処理（更新）

#### 主な関数

##### `UpdateCurrentInteractActor`

* `BP_FirstPersonCharacter` に実装
* 毎フレーム、カメラ前方のLineTrace結果から現在のインタラクト対象を更新する関数
* 判定処理をこの関数に集約し、UI更新処理と実行処理からLineTraceを分離するための関数

#### 処理フロー

1. カメラ前方へ `Line Trace By Channel` を実行
2. `Return Value` を Branch で分岐

#### LineTraceの構成

* Start：`FirstPersonCamera` の `World Location`
* Forward：`FirstPersonCamera` の `Forward Vector`
* 距離：300
* End：`Start + (Forward * 距離)`

#### 意味

* プレイヤーが見ている方向に向かって一定距離の判定を行う

##### Trace成功時

3. `Break Hit Result`
4. `Hit Actor` を取得
5. `Does Object Implement Interface` で `BPI_Interact` 実装有無を確認
6. Branch

* trueなら `CurrentInteractActor = Hit Actor`
* falseなら `CurrentInteractActor = None`

##### Trace失敗時

* `CurrentInteractActor = None`

#### 意味

* 毎フレームの判定結果を `CurrentInteractActor` に集約する
* UI更新処理と実行処理の両方がこの変数を参照する

### ■ プレイヤー側のインタラクト判定

#### 主な関数

#### 役割

* `UpdateCurrentInteractActor`：インタラクト対象の判定を担当
* `UpdateInteractPrompt`：UI表示の更新を担当
* Eキー処理：インタラクト実行を担当

##### `UpdateInteractPrompt`

* `BP_FirstPersonCharacter` に実装
* 毎フレーム、`CurrentInteractActor` を参照してUIを更新する関数

#### 処理フロー

1. `CurrentInteractActor` を `IsValid` で確認
2. trueの場合：

   * `GetInteractText(Message)` を呼ぶ（Target = CurrentInteractActor）
   * 返ってきたTextを `InteractPromptWidget` に渡す
   * `InteractPromptWidget` を `Visible`

3. falseの場合：

   * `InteractPromptWidget` を `Hidden`

#### 使用ノード

* `Is Valid`
* `GetInteractText (Message)`
* `SetPromptText`
* `Set Visibility`
---
### ■ 実行順序

#### Tick時

1. `UpdateCurrentInteractActor`
2. `UpdateInteractPrompt`

#### 意味

* 先に現在のインタラクト対象を更新し、その後UIを更新する
* UIは常に最新の `CurrentInteractActor` を基準に表示される

### ■ Eキー押下時の処理

#### `BP_FirstPersonCharacter` 側

* `CurrentInteractActor` を `IsValid` で確認
* trueなら `Interact(Message)` を送る（Target = CurrentInteractActor）
* falseなら何もしない

#### 意味

* プレイヤーが今見ている対象にのみインタラクトする
* 壁や床など、Interface未実装のActorには反応しない

### ■ 設計の核

* インタラクト対象はすべて `CurrentInteractActor` に集約する
* 判定・表示・実行を完全に分離する
* UIと実行処理は直接LineTraceを行わない
* すべての処理は `CurrentInteractActor` を基準に動く

### ■ 全体の処理フロー

1. Tickで `UpdateCurrentInteractActor` を実行
2. 現在見ている対象を `CurrentInteractActor` に保存
3. `UpdateInteractPrompt` がその値を参照してUI更新
4. Eキー押下時にその対象へ `Interact` を送る

#### 意味

* 判定 → 表示 → 実行 の流れが一貫している
* すべての処理が `CurrentInteractActor` を中心に動く

---

## 【UI構成】

### ■ ウィジェット一覧

#### `WBP_InteractPrompt`

* 用途：インタラクト可能オブジェクトを見ている時の表示
* 現在は固定表示ではなく、**対象に応じて文字を差し替える方式**

#### `WBP_UseFlashlight`

* 表示：「F：ライト使用」
* 用途：懐中電灯取得後表示

---

### ■ UI生成

* `BP_FirstPersonCharacter` の `BeginPlay` で生成
* `Add to Viewport`
* 初期状態：非表示

---

### ■ `WBP_InteractPrompt` の実装

#### 主なTextBlock

* `PromptText`

#### 主な関数

##### `SetPromptText`

* 引数：`DisplayText`（Text）
* `PromptText` に `SetText` する
* プレイヤー側から渡された文言を表示するために使用

---

### ■ UI制御（現状）

#### `InteractPromptWidget`

* `BP_FirstPersonCharacter` の `UpdateInteractPrompt` で制御
* `GetInteractText` の戻り値を `SetPromptText` に渡して表示内容を更新
* 対象がなければ `Hidden`
* 対象があれば `Visible`

#### `UseFlashlightPrompt`

* 懐中電灯取得時に表示

---

## 【プレイヤー状態設計】

* 通常状態（探索中）
* アイテム所持状態（`HasFlashlight`）
* インタラクト対象を見ている状態（CurrentInteractActorベース）

※ 将来的に Enum 管理へ拡張予定

---

## 【Blueprint責務設計】

### ■ `BP_FirstPersonCharacter`

* 入力処理
* インタラクト判定
* `UpdateInteractPrompt` によるUI更新
* 状態管理
* `Interact(Message)` の送信元

---

### ■ `BP_InteractActor`（基底クラス予定）

* インタラクト対象の共通クラス候補
* 将来的に共通処理を持たせる可能性あり

---

### ■ `BP_FlashlightPickup`

* `BPI_Interact` 実装済み
* `Interact` 時にプレイヤーへ取得状態を渡す
* `GetInteractText` で `Eで拾う` を返す
* 取得後に自身を削除

---

### ■ BP_LightSwitch（スイッチ）

#### 役割

* プレイヤーのインタラクトによってライトを操作する

---

#### コンポーネント構成

* `StaticMesh`（スイッチの見た目）
* `Box`（インタラクト判定用）

---

#### 変数

##### `TargetLight`（BP_Light 参照）

* 操作対象のライト

---
#### UI表示文言

##### `GetInteractText`

* 戻り値：`Text`
* `Eで操作` を返す
* `WBP_InteractPrompt` に表示される文言として使用

---
#### インタラクト処理

##### `Event Interact`

1. `TargetLight` を `IsValid` で確認
2. trueの場合：
   * `ToggleLight` を呼ぶ（Target = TargetLight）

---

#### 設計意図

* スイッチとライトを分離することで拡張性を確保
* レベル上で自由に組み合わせ可能にする

---

#### 使用方法（重要）

* レベル上で `BP_LightSwitch` を配置
* Detailsの `TargetLight` に `BP_Light` のインスタンスを設定する

※ この設定を行わないと動作しない

---

### ■ UI（Widget）

* 表示のみ担当
* ロジックは最小限
* 状態判断はプレイヤー側が管理
* 文言表示は `SetPromptText` で受け取る

---

## 【実装ルール】

### ■ 入力

* すべて `BP_FirstPersonCharacter` に集約

---

### ■ インタラクト

* プレイヤー → 対象を呼ぶ
* 対象 → プレイヤー操作は禁止
* 共通ルールは `BPI_Interact` にまとめる

---

### ■ UI

* `BeginPlay` で1回生成
* `Visibility` で制御
* 表示文言は対象側の `GetInteractText` で取得
* Widget自身は表示処理のみに寄せる

---

### ■ BeginPlay

* 1つに統一
* `Sequence` で分岐

---

### ■ 参照

* 最小限にする
* Destroy後の参照禁止
* プレイヤー側が現在の対象を動的に判定する

---

## 【現在の設計の特徴】

### ■ 良い点

* プレイヤー中心設計
* UI分離済み
* Interfaceベースの拡張しやすい構造
* `GetInteractText` により対象ごとの文言差し替えが可能
* `UpdateInteractPrompt` によりUI更新責務が明確
* ライト制御がActor単位に分離されている
* スイッチとライトが疎結合になっている
* 再利用可能な構造（インスタンス化前提）になっている

---

### ■ 改善余地

* インタラクト対象のハイライト表示を追加可能
* インタラクト距離の調整や制限を追加可能
* 長押し操作や条件付きインタラクトへの拡張が可能
* `BP_InteractActor` 基底クラス導入で共通化余地あり

---

## 【今後の実装予定】

1. ドア開閉
2. ホラー演出
3. スイッチのアニメーション（物理的動作）
4. ライト演出強化（色変化・異常挙動など）
---

## 【設計思想】

### ■ 重視すること

* 拡張性
* 責務分離
* プレイヤー中心設計

---

### ■ NG

* 各BPで入力処理を書く
* オブジェクトからプレイヤー操作
* UIに主要ロジックを書く

---

### ■ OK

* プレイヤーが制御
* オブジェクトは受動
* UIは表示中心
* 表示文言は対象側から取得

---

