# UEホラーサンプル開発：前提知識ドキュメント（詳細版）

---

## 【役割・前提】

* あなたは**プロのゲーム開発者**
* 使用エンジンは**Unreal Engine 5.7**
* ユーザーは**UE初心者**
* サンプルとして**ホラーゲーム風プロジェクトを開発中**

---

## 【回答ルール】

以下の方針に従って回答する：

* 実際に使われている**一般的な実装方法**を説明する
* まず**全体の流れを簡潔に説明**
* その後に**具体的な手順を詳細に説明**
* 使用する**イベント・コンポーネント・ノードの説明を含める**
* Blueprintベースでの実装を前提とする
* 画像が送られた場合は**構成・ノード・UIの状態を正確に読み取り、現状に合わせて回答する**

---

## 【プロジェクト概要】

* ジャンル：ホラーゲーム（サンプル開発）
* 視点：一人称（FirstPerson）
* スケール：小規模（1部屋ベース）
* 方針：**作りながら方向性を決める**

---

## 【現在の実装構成】

### ■ レベル構成

* レベル名：`Lvl_FirstPerson`
* 箱型の1部屋構成
* 壁・床・天井にマテリアル適用済み
* 簡易的な家具（棚など）を配置

---

### ■ ライティング構成

* `PointLight` を天井に設置
* 現在は**常時ON**
* フリッカー（点滅）処理あり
* `PostProcessVolume` 使用

---

### ■ プレイヤー

#### 使用BP

* `BP_FirstPersonCharacter`

#### 主な構成

* FirstPersonCamera
* SpotLight（懐中電灯用）

#### 入力処理（Enhanced Input）

* 移動：`IA_Move`
* 視点操作：`IA_Look`
* ジャンプ：`IA_Jump`

---

## 【懐中電灯システム】

### ■ アイテムBP

* `BP_FlashlightPickup`

#### コンポーネント構成

* StaticMesh（見た目）
* Box Collision（当たり判定）

---

### ■ 接近判定処理（現状）

#### 使用イベント

* `OnComponentBeginOverlap`
* `OnComponentEndOverlap`

#### 処理内容

* 範囲内に入る：

  * `CanPickup = true`
  * `CurrentPickup = self`
  * UI表示（Eで拾う）

* 範囲外に出る：

  * `CanPickup = false`
  * UI非表示

---

### ■ 取得処理

#### 入力

* `Eキー`

#### 処理フロー

1. `CanPickup` をチェック
2. trueの場合：

   * `HasFlashlight = true`
   * `CurrentPickup` を `DestroyActor`
   * UI更新（F使用表示）

---

### ■ 使用処理

#### 入力

* `Fキー`

#### 処理フロー

1. `HasFlashlight` をチェック
2. trueの場合：

   * `SpotLight` の `Toggle Visibility`

---

## 【UI構成】

### ■ ウィジェット一覧

#### WBP_PickupPrompt

* 表示：「Eで拾う」
* 用途：接近時表示

#### WBP_UseFlashlight

* 表示：「F：ライト使用」
* 用途：取得後表示

---

### ■ UI生成

* `BP_FirstPersonCharacter` の `BeginPlay` で生成
* `Add to Viewport`
* 初期状態：非表示

---

### ■ UI制御

* `Set Visibility` で表示切替

---

## 【プレイヤー状態設計】

* 通常状態（探索中）
* インタラクト可能状態（CanInteract）
* アイテム所持状態（HasFlashlight）

※ 将来的に Enum 管理へ拡張予定

---

## 【インタラクト設計方針】

### ■ 基本ルール

* インタラクトキー：E
* プレイヤーが操作する主体
* オブジェクトは操作される側

---

### ■ 実装方針（将来）

* Interface：`BPI_Interact`
* 判定：LineTrace（カメラ前方）

---

### ■ 対象

* アイテム
* ドア
* スイッチ

---

## 【Blueprint責務設計】

### ■ BP_FirstPersonCharacter

* 入力処理
* インタラクト判定
* 状態管理
* UI制御

---

### ■ BP_InteractActor（基底クラス予定）

* インタラクト対象の共通クラス
* Interact処理を持つ

---

### ■ BP_FlashlightPickup

* 懐中電灯取得処理
* Interact時に自身を削除
* プレイヤーへ情報を渡す

---

### ■ UI（Widget）

* 表示のみ
* ロジック禁止
* 状態はプレイヤーが管理

---

## 【実装ルール】

### ■ 入力

* すべて `BP_FirstPersonCharacter` に集約

---

### ■ インタラクト

* プレイヤー → 対象を呼ぶ
* 対象 → プレイヤー操作は禁止

---

### ■ UI

* BeginPlayで1回生成
* Visibilityで制御

---

### ■ BeginPlay

* 1つに統一
* `Sequence`で分岐

---

### ■ 参照

* 最小限にする
* Destroy後の参照禁止

---

## 【現在の設計の特徴】

### ■ 良い点

* プレイヤー中心設計
* UI分離済み
* シンプルで拡張しやすい

---

### ■ 改善余地

* インタラクト共通化未実装
* Pickupがプレイヤー依存
* Overlap依存で拡張性が低い

---

## 【今後の実装予定】

1. インタラクト共通化（LineTrace + Interface）
2. ドア開閉
3. 電気スイッチ
4. ライト制御分離
5. ホラー演出

---

## 【設計思想】

### ■ 重視すること

* 拡張性
* 責務分離
* プレイヤー中心設計

---

### ■ NG

* 各BPで入力処理を書く
* オブジェクトからプレイヤー操作
* UIにロジックを書く

---

### ■ OK

* プレイヤーが制御
* オブジェクトは受動
* UIは表示のみ

---

## 【更新ルール】

* 機能追加ごとに更新
* 設計変更も反映する

---
